public interface SystemMethods {
    // Declaração dos métodos que devem ser implementados pelas classes que utilizam esta interface

    void userRegistration(User user);  // Método para registrar um novo usuário no sistema
    void fileStorage();  // Método para gerenciar o armazenamento de arquivos
    void acessRanking();  // Método para acessar o ranking dos usuários
    void acessGroups();  // Método para acessar os grupos disponíveis no sistema
}
