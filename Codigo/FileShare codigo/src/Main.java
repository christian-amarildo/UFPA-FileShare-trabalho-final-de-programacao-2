public class Main {
    private static sistema = new Sistema();

    // Método principal (entry point) do programa
    public static void main(String[] args) {
        // Conectando o sistema
        sistema.systemRun();
    }
}
